/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['gaguugqrvzpdimwhwien.supabase.co'], // Add your Supabase domain here
    },
};

export default nextConfig;
